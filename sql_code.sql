CREATE TABLE Movies (
    id int primary key,
    title varchar(1000),
    describe varchar(MAX),
    datepublished varchar(255),
    genre varchar(255),
    rating int,
    votes int,
    img varchar(255),
    years varchar(255),
    actor varchar(MAX),
    director varchar(MAX),
)